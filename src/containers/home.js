import React from 'react';
import Layout from "../hoc/Layout";

const Home = () => {
    return (
        <Layout>
            <h1>SA React Simplest Boilerplate</h1>
        </Layout>
    );
};

export default Home;