import React from 'react';

const Layout = props => {
    // const { classes } = props;

    return (
        <div className={''}>

            {props.children}


        </div>
    );
};

export default Layout;
